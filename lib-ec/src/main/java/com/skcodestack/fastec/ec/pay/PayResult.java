package com.skcodestack.fastec.ec.pay;import android.text.TextUtils;public class PayResult {    //支付宝支付成功状态值    public static final String ALIPAY_SUCCESS_STATUS = "9000";    //订单处理中    public static final String ALIPAY_PAYING_STATUS = "8000";    //订单支付失败    public static final String ALIPAY_FAIL_STATUS = "4000";    //取消支付    public static final String ALIPAY_CANCLE_STATUS = "6001";    //支付网络错误    public static final String ALIPAY_CONNECTION_ERROR_STATUS = "6002";    private String resultStatus;    private String result;    private String memo;    public PayResult(String rawResult) {        if (TextUtils.isEmpty(rawResult))            return;        String[] resultParams = rawResult.split(";");        for (String resultParam : resultParams) {            if (resultParam.startsWith("resultStatus")) {                resultStatus = gatValue(resultParam, "resultStatus");            }            if (resultParam.startsWith("result")) {                result = gatValue(resultParam, "result");            }            if (resultParam.startsWith("memo")) {                memo = gatValue(resultParam, "memo");            }        }    }    @Override    public String toString() {        return "resultStatus={" + resultStatus + "};memo={" + memo                + "};result={" + result + "}";    }    private String gatValue(String content, String key) {        String prefix = key + "={";        return content.substring(content.indexOf(prefix) + prefix.length(),                content.lastIndexOf("}"));    }    /**     * @return the resultStatus     */    public String getResultStatus() {        return resultStatus;    }    /**     * @return the memo     */    public String getMemo() {        return memo;    }    /**     * @return the result     */    public String getResult() {        return result;    }}